# @package _global_

defaults:
  - default
  - override /model: fast3r

task_name: fast3r_habitat_larger_decoder_views8


data:
  num_views: 8
  data_module:
    batch_size_per_device: 2

model:
  net:
    decoder_args:
      embed_dim: 1024
      num_heads: 16
      depth: 24
